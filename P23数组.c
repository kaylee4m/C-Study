#include <stdio.h>

//数组要有同样的数据类型，使用过程中要保留原始数据
//数组是有序的集合
//数组的每一个数据有数组元素和下标变量
//数组元素有序不是按元素大小顺序，是位置有序

//一维数组 int a[10] 表示定义一个整型数组，数组名为a，数组有10个元素，10个元素都是整型变量
//下标从0开始
//c语言不允许对数组的大小作动态定义，即数组的大小不依赖于程序运行过程中变量的值。
//没有0个数组的数组，不能用圆括号，不能用变量说明数组大小
//一维数组在内存中的存放： 一维数组int mark[100];从mark0到mark99是从低地址到高地址
//引用：数组元素也是一个变量，一般是数组名[下标]
//可以a[i+j] a[i++]
//数组元素一般也称为下标变量
//不能用一个语句输出整个数组
//定义数组用 数组名[常量表达式]
//int a[10] //长度是10
//t=a[6] //引用数组中序号为6的元素，6不是数组的长度

