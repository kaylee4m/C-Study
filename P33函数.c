#include <stdio.h>

/* 实参和形参的类型应该相同或者赋值兼容。
*实参向对形参的数据传递是值传递，单项传递，只由实参传到形参
*函数的返回值是通过函数中的return语句获得的
*return把调用函数中的一个确定值带回主调函数中
*一个函数中可以有一个以上的return语句，执行到哪一个，哪一个语句起作用
*函数的返回值应该是一个确定的类型，在定义函数时指定函数返回值的类型
*在C中不加类型说明的函数自动按整型处理
*在定义函数是一般和return中的表达式类型一致
*对于不带回值的函数，应该用空类型void
*/